# Test Scope

## Defaults

- Use unit tests for pure logic.
- Use slice tests when one framework layer matters.
- Use integration tests when wiring or infrastructure behavior matters.

## Review checks

- Is the test broader than needed?
- Is the test too narrow to catch the real regression?
